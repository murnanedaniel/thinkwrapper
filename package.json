{
  "name": "thinkwrapper",
  "version": "1.0.0",
  "description": "AI Newsletter Generator",
  "scripts": {
    "start": "echo 'Application is running'",
    "heroku-postbuild": "cd client && npm ci && npm run build && cp -r dist ../app/static",
    "build": "cd client && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
} 